<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gui.assignment">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gui">
        <activity android:name=".HomePangacaraActivity"></activity>
        <activity android:name=".MenuPangacaraActivity" />
        <activity android:name=".PengaturanPangacaraActivity" />
        <activity android:name=".SettingsActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDv7xHsslzLFvngqbg6fuOn3d5DPoJau84" />

        <activity android:name=".ChattingActivity" />
        <activity android:name=".JanjiActivity" />
        <activity android:name=".ProfilePangacaraActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".SuccesActivity" />
        <activity android:name=".SelesaiDaftarActivity" />
        <activity android:name=".TermsConditionActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".DaftarPengacaraActivity" />
        <activity android:name=".Daftar" />
        <activity android:name=".SignInPengacaraActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".WelcomActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>